Import("*")

SimObject("Accelerator.py", sim_objects=["GrayscaleAccelerator"])

Source("accelerator_wrapper.cc")

# The accelerator model is declared by ../model/SConscript, not here: a Source()
# path with ".." escapes gem5's per-directory variant-dir mapping and fails to
# resolve. No CPPPATH either — cross-directory includes are written relative to
# their own file so they also resolve in the generated param file (see that file).
